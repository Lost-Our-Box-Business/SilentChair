// Per-business Task Board — Kanban view (Planned / In Progress / Blocked by User / Completed)
// Phase 2 implementation

export default function BusinessTasksPage({ params }: { params: { businessId: string } }) {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold mb-2">Task Board</h1>
      <p className="text-muted-foreground">Task board coming in Phase 2.</p>
    </div>
  );
}
