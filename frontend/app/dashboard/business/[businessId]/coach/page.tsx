// Coach scheduling — Calendly embed + session history + AI briefing preview
// Phase 9 implementation

export default function CoachPage({ params }: { params: { businessId: string } }) {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold mb-2">Business Coach</h1>
      <p className="text-muted-foreground">Coach scheduling coming in Phase 9.</p>
    </div>
  );
}
