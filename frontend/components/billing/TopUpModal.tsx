// TopUpModal — modal dialog for purchasing additional AI spend balance
// Input: dollar amount (minimum $10) → calls POST /api/billing/topup → redirects to Stripe Checkout
// Phase 5 implementation

export function TopUpModal() {
  return null;
}
