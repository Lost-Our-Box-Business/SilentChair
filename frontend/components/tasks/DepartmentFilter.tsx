// DepartmentFilter — row of chip buttons above the Task Board to filter by department
// "All" chip is always first; one chip per unique department in the task list
// Phase 2 implementation

export function DepartmentFilter() {
  return null;
}
