// TaskBoard — Kanban board with four columns: Planned, In Progress, Blocked by User, Completed
// Columns map to tasks.status: 'planned' | 'in_progress' | 'awaiting_approval' | 'completed'
// Phase 2 implementation

export function TaskBoard() {
  return null;
}
