// TaskCard — individual task card for the Kanban board
// Shows: title, department label (color-coded), cost (on completed), Approve button (on blocked)
// Phase 2 implementation

export function TaskCard() {
  return null;
}
