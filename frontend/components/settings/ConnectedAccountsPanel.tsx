// ConnectedAccountsPanel — list of platform connections with Connect/Disconnect buttons
// Platforms: Instagram, Facebook, Google Ads, Meta Ads, Buffer, WordPress, LinkedIn, X
// Connect triggers OAuth flow via GET /api/settings/connected/{businessId}/oauth/start
// Phase 8 implementation

export function ConnectedAccountsPanel() {
  return null;
}
